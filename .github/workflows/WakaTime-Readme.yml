name: WakaTime Readme

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 19 * * *'

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: liuyingqiao2019/liuyingqiao2019@main
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}